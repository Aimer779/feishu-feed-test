"""
信息总结器 (Summarizer)
================================================================================

【在流程中的作用】
负责对接上游抓取到的原始文章，调用 AI（LLM）进行智能化处理。
它是连接"原始数据"与"卡片展示"的桥梁，核心任务是：
  1. 阅读 fetcher.py 输出的原始文章列表
  2. 调用 AI 对每篇文章进行摘要提取
  3. 按预定义的 7 大主题（大厂&融资、模型&论文、产品&开源、编程&架构、
     增长&自媒体、独立开发、观点&争议）进行分类
  4. 对重复或相似内容做去重/合并
  5. 输出符合 card_builder.py 要求的 categories 结构化数据

本模块**不涉及**网络抓取、HTTP 请求、卡片 JSON 组装或飞书消息推送。

================================================================================

【上游】
- fetcher.py (信息抓取器)
  提供已经清洗过的原始文章列表，包含 title、url、content、author、
  published_at 等字段。

================================================================================

【下游】
- card_builder.py (卡片构建与发送器)
  接收 summarizer.py 生成的 categories 数据，动态组装成飞书卡片 JSON，
  并通过 Webhook 推送到群聊。

================================================================================

【期望输出格式】
AI 总结完成后，应输出如下结构：

[
    {
        "name": "大厂&融资",
        "summary": "科技巨头新一轮融资潮涌动，AI 赛道估值持续走高",
        "items": [
            {
                "title": "某 AI 独角兽完成 10 亿美元融资",
                "summary": "估值突破 300 亿美元",
                "source": "Bloomberg",
                "url": "https://bloomberg.com"
            },
            ...
        ]
    },
    ...
]

注意：
  - 如果某主题下没有任何文章，该主题**不应出现在**输出列表中
  - items 的长度不限，可为 1~N 条
  - source 建议使用平台名或原始站点名，用于卡片中链接的显示文本
  - **AI 输出中不应包含 emoji 字段**，emoji 由 card_builder.py 根据主题名硬编码映射

================================================================================
"""
